
Development Tools Guide - Noah Billedo:

Pre-Push Git Hooks Install
Tool to prevent failed deployments from being pushed, should mitigate failed Github Page Commits. 
Runs local CI checks before allowing push.

If devcontainer did not properly install run the following commands in your terminal.

chmod +x ./scripts/install-hooks.sh
./scripts/install-hooks.sh
